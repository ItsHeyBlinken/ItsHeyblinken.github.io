# Progress

**Append-only log of completed work and milestones.** Newest entries at the bottom.

## Session log

- **2026-04-29 — Pricing page positioning:** Reworked `pricing.html` for agency-style positioning: Launch / Growth / Signature website tiers with higher project investments, Site Care (monthly/yearly/on demand) copy that separates infrastructure from value, Complete packages aligned to tiers + yearly care, ecommerce as starting-at investments with discovery framing, consistent “Request a quote” CTAs, updated meta/JSON-LD, FAQ refresh. `pricing.css`: added `.pricing-subtitle`, removed pulsing popular-card animation.
- **2026-04-29 — Pricing feature list flex fix:** Wrapped all `.pricing-features` list copy in `<span class="pricing-feature-text">` so inline `<strong>` is not laid out as a separate flex item (fixes broken “Up to 5/10 pages” lines). Updated `pricing.css`: `align-items: flex-start`, `gap`, `.pricing-feature-text` with `flex:1; min-width:0`, icon `flex-shrink:0` + slight top offset.
- **2026-04-29 — Pricing SaaS-style copy pass:** Reframed visible `pricing.html` body copy (head meta/JSON-LD unchanged): plans/tiers/subscription vocabulary, growth-stage subtitles on website tiers, stronger middle-column bias (Growth + commerce ops), shorter scannable bullets, activation bundles naming, Site Care as retention/performance layer, FAQ + CTA aligned to risk reduction; CTAs to “Request this plan/bundle” / “Email to confirm your plan.” Pricing amounts and section IDs preserved.
- **2026-04-11 — Tailwind production setup:** Replaced Tailwind CDN with local CLI build (`tailwind-built.css`), `tailwind.config.js`, `src/tailwind-input.css`, npm scripts, `.gitignore` for `node_modules/`. Updated `index.html`, `templates.html`, `pricing.html` to link built CSS and remove CDN + inline `tailwind.config`.
- **2026-04-11 — Coolify verification:** User confirmed deployment succeeds with **no console errors** (Tailwind production warning resolved).
- **2026-04-11 — Memory Bank:** Initialized `memory-bank/` with `projectbrief.md`, `productContext.md`, `activeContext.md`, `systemPatterns.md`, `techContext.md`, `progress.md`.
- **2026-04-11 — Hosting clarified:** Production is **Coolify** pulling from **GitHub**; **GitHub Pages is not used.** Updated Memory Bank (`projectbrief`, `activeContext`, `techContext`).

- **2026-05-05 — Sitemap dedupe (Search Console fix):** Removed `https://builtbyblinken.com/index.html` from `sitemap.xml` to resolve a Google Search Console "Alternate page with proper canonical tag" notice. The bare-domain `https://builtbyblinken.com/` URL is the canonical homepage (per `<link rel="canonical">` and `.htaccess` 301 from `/index.html` → `/`); listing `/index.html` separately in the sitemap was telling Google to crawl a URL whose own `<head>` declares it non-canonical. Sitemap now lists three URLs only: `/`, `/templates.html`, `/pricing.html`. After Coolify redeploys, resubmit the sitemap in Search Console (Indexing → Sitemaps) so Google picks up the change quickly. **Watch:** `/Site Templates/` demo HTML files have no `noindex` meta and no canonical tags; `.htaccess` 404s direct access if Apache is the web server, otherwise they may be publicly served and discoverable. Confirm Coolify's web server (Apache vs. nginx/Caddy) before deciding whether to add `noindex` meta to those demos or `Disallow: /Site Templates/` to `robots.txt`.

- **2026-05-05 — Pricing repositioning (Signature + Care on demand):** After the agency redesign repositioned the brand, repriced two tiers to better match agency-quality scope and market rates while keeping volume tiers (Launch, Growth) unchanged. Signature website plan: **$5,495 → $6,995** (updated visible card price, comparison-table "Project investment" row, and JSON-LD `Offer` `price` value to `"6995"`). Signature + Care activation bundle: **$6,499 → $7,995** (preserves ~$299 bundle savings vs list, in line with discount shape on Launch + Care and Growth + Care). Care on demand hourly: **$95 → $125** (closer to agency floor of $125–$200/hr). Also updated the homepage FAQ that quoted the Signature price (`index.html` `faq-panel-1`). Launch ($1,495), Growth ($2,995), Site Care Monthly ($129), Site Care Yearly ($1,299), Launch + Care bundle ($2,699), Growth + Care bundle ($4,199), and all Commerce starting-at prices were intentionally left unchanged. JSON-LD `Offer` blocks for Launch and Growth untouched; only the Signature `price` value changed. Verified no stale `$5,495` / `$6,499` / `$95` references remain in `index.html`, `pricing.html`, or supporting files.

- **2026-05-05 — Agency redesign across all 3 main pages:** Evolved the dark-indigo brand into an agency-style site to attract more small-business clients. Foundation: added Fraunces display serif (Google Fonts) and `font-display` Tailwind utility (`tailwind.config.js`); added shared design-system tokens to `style.css` (`.eyebrow`, `.section-index`, `.bg-gradient-soft`, `.industry-pill`, `.stat-block` family, `.btn-cta`, `.btn-ghost`, `.differentiator-card`, `.process-timeline`, `.work-card`, `.hero-stack`, `.compare-table`, `.filter-btn`, `.template-card-chip`). **Homepage (`index.html`):** rewritten with eyebrow + Fraunces hero (`Custom websites for small businesses ready to look the part online.`), CSS-only layered template-stack visual, industries-served pill row, agency stats bar, refined 3-card services (no center image), `Recent work` section linking to 3 live demos (Pixel Perfect, 5 Real Estate, Vinyl Vault), `Why work with us` differentiator grid, redesigned process timeline with desktop connector line, replaced `Rebecca Lewis`/`Michael Torres` testimonials with a generic "How we work" social-proof block (no fake names), expanded FAQ to 8 questions including pricing and redesign Qs, refreshed final CTA. **Templates page (`templates.html`):** switched from `style1.css` to `style.css` for shared tokens; new agency hero; segmented-control filter bar with per-category counts and live result-count text; templates regrouped under 4 category sections (Professional / Small business / Portfolio / Retail & e-commerce), each with eyebrow + Fraunces section heading; always-visible category chip on every card; new "Don't see a fit?" custom-build CTA at the bottom; JS rewritten to toggle category sections plus update result count. **Pricing page (`pricing.html`):** new agency hero; tabs wrapped in unified segmented-control container; `Most popular` Growth tier upgraded with accent ring/glow + gradient ribbon; **new feature comparison table** for Launch vs Growth vs Signature inside the `#website-design` tab; aggressive SEO-friendly copy refresh on every section's intro paragraph and the FAQ; FAQ expanded from 4 to 6 items (added `Do you offer payment plans for small businesses?` and `What's included with SEO on every plan?`); cross-section `data-target-tab` links from the comparison footnote drive the existing tab JS. **All pages:** keyword-rich `<title>` and `meta description`, refreshed OG/Twitter metadata, refreshed `WebDesignAgency` and `CollectionPage` JSON-LD descriptions (pricing JSON-LD prices, tier names, and `Offer` blocks deliberately preserved). `sitemap.xml` lastmod bumped to 2026-05-05 across all four URLs. Tailwind rebuilt via `npm run build:css` (Inter + Fraunces font families now both reflected in `tailwind-built.css`).

- **2026-06-25 — Pricing sync to Market Entry + new tier/messaging (per `cursor_fix_pricing.md`):** Resolved pricing drift by standardizing the site on the lower "Market Entry" rates and adding strategic positioning. **`pricing.html`:** JSON-LD `Offer` prices updated Launch `1495→995`, Growth `2995→1995`, Signature `6995→3995`; comparison-table "Project investment" row updated to `$995 / $1,995 / $3,995`; activation bundles repriced Launch+Care `$2,495→$1,995`, Growth+Care `$3,495→$2,995`, Signature+Care `$5,495→$4,995`; Site Care Monthly description changed from "small content/layout tweaks inside your monthly allowance" to "up to 30 minutes of content/layout updates per month (non-rolling)"; **added new "Infrastructure Only" tier** ($29/mo or $295/yr — managed hosting, SSL, .com domain renewal, weekly backups; excludes content/software updates & priority support) as the first card in the Site Care section, widening that grid from `md:grid-cols-3` to `grid-cols-1 md:grid-cols-2 lg:grid-cols-4` (mobile-friendly); **added introductory-pricing banner** to the pricing hero ("Limited-time introductory pricing for our first 5 partner projects…"). **`index.html`:** homepage FAQ prices synced to `$995 / $1,995 / $3,995` and commerce `$4,500+ → $3,995+`; footer About paragraph adds a McKinney, TX local-portfolio + introductory-pricing note. Site Care Monthly ($129), Yearly ($1,299), Care on demand ($125), and pricing-card prices were already at Market Entry rates and left unchanged. Tailwind rebuilt via `npm run build:css` (exit 0). Verified no stale `$1,495 / $2,495 / $3,495 / $5,495 / $6,995 / $4,500` references remain in the HTML. **Follow-up same session:** reconciled the "infrastructure from $X/yr after year one" line on all three website plan cards from `$195/yr → $295/yr` so it matches the new Infrastructure Only tier ($295/yr) — single consistent infrastructure number sitewide (no rebuild needed; copy-only change). **Follow-up same session (framing pass):** `pricing.html` Site Care section eyebrow/tab renamed to "Hosting & Site Care"; intro copy now explicitly positions Infrastructure Only vs Site Care tiers; `index.html` Site Care service card and maintenance FAQ aligned to "up to 30 minutes of content/layout updates per month (non-rolling)" with FAQ link to `#maintenance` for Infrastructure Only.

- **2026-07-14 — Recruiting-Template mobile responsiveness:** Rebuilt the sample profile mobile nav into a dedicated stack panel with aria-expanded control, backdrop, Escape/resize close, and hamburger-to-X animation; tightened hero metrics, section spacing, stats/video controls, and CTA touch targets for small screens; rebuilt `Recruiting-Template/dist/style.css`. Mirrored a real mobile menu in the React scaffold `StickyNav`.

- **2026-07-14 — Recruiting packet PDF preview:** Kept the sample CTA as an on-screen Letter-sized profile-sheet preview (`Recruiting-Template/recruiting-packet.html`) instead of a real PDF download; button copy updated to “Preview recruiting packet (PDF-style).”

- **2026-07-14 — Sample demo URL fix:** Pointed agency demo CTAs to `./Recruiting-Template/index.html` (not the bare directory) so hosts that do not auto-serve directory indexes load the profile instead of a file listing.

- **2026-07-14 — Private athlete demo scrub:** Removed all agency links to `skittles-softball.bytesbyblinken.com`. Converted `Recruiting-Template/` into a fictional Jordan Rivers volleyball sample with SVG placeholders only; deleted personal photos/videos/PDF; scrubbed nested Memory-Bank and contact fields (no real email/phone). Demo CTAs on `athlete-recruiting.html`, `index.html`, `templates.html`, and `pricing.html` now point to `./Recruiting-Template/`. Left root PRD/marketing brief unchanged per request. Clarified: her live recruiting site stays online at its own domain; it is simply not used as the agency product demo.

- **2026-07-14 — Athlete process timeline alignment:** On `#athlete-process`, replaced translucent Tailwind step badges with solid `.process-step-badge` pills and recalculated the desktop connector line to `top: 3.5rem` (card padding 2rem + half of 3rem badge) so the line meets badge centers and no longer shows through the circles.

- **2026-07-14 — Athlete recruiting page spacing fixes:** On `athlete-recruiting.html`, increased How-it-works eyebrow→heading spacing, process-card internal bottom padding, and horizontal gaps between the four steps. Fixed All-In plans disclaimer overlapping MVP/All-Star CTAs by giving `#athlete-pricing .athlete-pricing-grid` bottom clearance and dialing back `.pricing-popular` scale/`z-index` collision via scoped rules in `style.css` (does not change `pricing.html` card treatment).

- **2026-07-14 — Athlete recruiting profile product launch scaffold:** Added `athlete-recruiting.html`, an agency-styled Collegiate Bound Profiles value page explaining the recruiting opportunity, core profile sections, guardian-first privacy model, managed workflow, live Skittles Softball demo, FAQs, athlete-specific email CTAs, SEO/social metadata, and `Service`/`Offer` structured data. Added a homepage specialty callout and templates-page promotion while intentionally preserving the standard catalog at 18 templates. Added an accessible `pricing.html#athlete-profiles` tab with linked tabpanels, keyboard navigation, hash routing, and synchronized offers: The MVP ($249 setup + $25/month, domain year one then $60/year, 3 videos, quarterly non-rolling update, supplied PDF link) and The All-Star ($299 setup + $45/month, domain while subscribed, 5 videos, monthly non-rolling update, custom matching PDF). Updated pricing metadata, `tailwind.config.js`, `sitemap.xml`, and `robots.txt`. Created a separate source-only sport-agnostic React/TypeScript/Tailwind v4 scaffold at sibling path `../Athlete-Recruiting-Profile-Template/` with fictional placeholder data, local SVG assets, TypeScript types, JSON Schema, runtime validation, accessible responsive sections, filterable video placeholders, tabbed statistics, guardian-first contact, tests, and customization documentation. Confirmed the existing nested `Recruiting-Template` live-demo repository was restored to a clean state after a target-path conflict. **Intentionally not performed:** npm install, lockfile generation, lint/test/type-check/build commands, `tailwind-built.css` regeneration, Git initialization/commit/push, Coolify configuration, DNS, or deployment. IDE diagnostics reported no errors; executable verification remains deferred until the user runs the documented commands.

## Earlier work (from project history, summarized)

- Email-first CTAs and copy; hero/layout iterations; semantic `h1` ids; removed placeholder footer social icons; FAQ accordions; scoped `main h1` CSS to cooperate with Tailwind utility classes.
